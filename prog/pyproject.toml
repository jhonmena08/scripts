[project]
name = "prog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "topo",
]

[tool.uv.sources]
topo = { path = "../topo/dist/topo-1.0.3.tar.gz" }
